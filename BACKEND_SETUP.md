# Backend Integration Guide

## Статус

✅ **Backend репозиторий создан**: [https://github.com/nicshik/mathstat-quiz-backend](https://github.com/nicshik/mathstat-quiz-backend)

⏳ **Требуется**: Развернуть backend на Railway и обновить `app.js`

---

## Шаг 1: Разверните backend на Railway

### Вариант A: Через веб-интерфейс Railway (3 минуты)

1. **Зайдите на Railway**  
   https://railway.app → Login через GitHub

2. **Создайте новый проект**  
   Dashboard → **"New Project"** → **"Deploy from GitHub repo"**

3. **Выберите репозиторий**  
   `nicshik/mathstat-quiz-backend`

4. **Добавьте переменные окружения**  
   Перейдите: **Variables** → **Raw Editor** → Вставьте:

   ```env
   EMAIL_USER=ваш-gmail@gmail.com
   EMAIL_PASSWORD=xxxx xxxx xxxx xxxx
   RECIPIENT_EMAIL=shikhirev.nn@phystech.edu
   NODE_ENV=production
   ```

   **Важно:** `EMAIL_PASSWORD` это **App Password** из Gmail (16 символов)!

   #### Как получить Gmail App Password:
   
   a. https://myaccount.google.com/security → Enable **2-Step Verification**  
   b. https://myaccount.google.com/apppasswords → **"Select app: Mail"** + **"Select device: Other"**  
   c. Введите имя: "Railway"  
   d. Скопируйте 16-символьный пароль (с пробелами!)  
   e. Вставьте в `EMAIL_PASSWORD`

5. **Сгенерируйте домен**  
   **Settings** → **Networking** → **Generate Domain**

6. **Скопируйте URL**  
   Например: `https://mathstat-backend-production.up.railway.app`

---

## Шаг 2: Обновите frontend для использования Railway backend

### Откройте `app.js` и найдите строку (~98):

```javascript
// Send to backend
const response = await fetch('/api/feedback', {
```

### Замените на:

```javascript
// ⚠️ ВСТАВЬТЕ СЮДА ВАШ RAILWAY URL!
const BACKEND_URL = 'https://your-railway-url.up.railway.app';

// Send to backend
const response = await fetch(`${BACKEND_URL}/api/feedback`, {
```

**Пример с реальным URL:**

```javascript
const BACKEND_URL = 'https://mathstat-backend-production.up.railway.app';

const response = await fetch(`${BACKEND_URL}/api/feedback`, {
    method: 'POST',
    headers: {
        'Content-Type': 'application/json'
    },
    body: JSON.stringify(feedbackPayload)
});
```

---

## Шаг 3: Закоммитьте изменения

```bash
git add app.js BACKEND_SETUP.md
git commit -m "feat: integrate Railway backend for feedback"
git push origin main
```

GitHub Pages автоматически обновится через 1-2 минуты.

---

## Проверка

### 1. Проверьте backend здоровье

Откройте в браузере:
```
https://your-railway-url.up.railway.app/api/health
```

Должны увидеть:
```json
{
  "status": "ok",
  "timestamp": "2025-12-26T...",
  "emailReady": true
}
```

### 2. Проверьте логи Railway

Railway Dashboard → **Deployments** → кликните на последний деплой → **Logs**

Должны видеть:
```
┌──────────────────────────────────────────────────┐
│ Mathstat Quiz Backend API
├──────────────────────────────────────────────────┤
│ Server running on port: 3000
│ Environment: production
│ CORS enabled for: https://nicshik.github.io
└──────────────────────────────────────────────────┘
Email service ready
```

### 3. Тест обратной связи

1. Откройте: https://nicshik.github.io/mathstat-exam-quiz/
2. Пройдите квиз, ответьте неправильно
3. Нажмите **"Сообщить о неточности"**
4. Введите тестовое сообщение
5. Нажмите **"Отправить"**
6. Должны увидеть: "Спасибо за обратную связь!"
7. Проверьте email: `shikhirev.nn@phystech.edu`

---

## Troubleshooting

### ❌ "Ошибка при отправке"

**Причина:** Frontend не может связаться с backend.

**Решение:**
1. Проверьте, что Railway URL правильно вставлен в `app.js`
2. Проверьте `/api/health` endpoint (должен работать)
3. Откройте DevTools (F12) → Console → проверьте ошибки CORS

### ❌ CORS ошибка в консоли

**Причина:** Backend не разрешает запросы с GitHub Pages.

**Решение:**
1. Убедитесь, что в `server.js` есть:
   ```javascript
   app.use(cors({
       origin: 'https://nicshik.github.io',
       methods: ['GET', 'POST', 'OPTIONS']
   }));
   ```
2. Если изменили `server.js` → закоммитьте → Railway автоматически передеплоит

### ❌ "Email service not ready" в логах Railway

**Причина:** Неправильные Gmail credentials.

**Решение:**
1. Railway Dashboard → **Variables**
2. Проверьте `EMAIL_USER` (должен быть полный email: `user@gmail.com`)
3. Проверьте `EMAIL_PASSWORD` (должен быть App Password, 16 символов с пробелами)
4. Убедитесь, что 2FA включена в Gmail
5. После изменения переменных → Railway автоматически перезапустит сервер

### ❌ Email не приходит

**Решение:**
1. Проверьте папку **Спам**
2. Добавьте `EMAIL_USER` в контакты Gmail
3. Проверьте логи Railway на наличие ошибок отправки email
4. Убедитесь, что `RECIPIENT_EMAIL=shikhirev.nn@phystech.edu`

---

## Архитектура (текущая)

```
┌─────────────────────────────────────┐
│  FRONTEND                           │
│  GitHub Pages                       │
│  nicshik.github.io/mathstat-exam-quiz │
│                                     │
│  Files:                             │
│  - index.html                       │
│  - app.js (с Railway URL)           │
│  - quizData.json                    │
│  - styles.css                       │
└──────────────┬──────────────────────┘
               │
               │ HTTPS POST /api/feedback
               │ (CORS enabled)
               ▼
┌─────────────────────────────────────┐
│  BACKEND                            │
│  Railway                            │
│  mathstat-backend.up.railway.app    │
│                                     │
│  Files:                             │
│  - server.js (Express + CORS)       │
│  - package.json                     │
│  - .env (в Railway Variables)       │
└──────────────┬──────────────────────┘
               │
               │ SMTP (Gmail)
               ▼
┌─────────────────────────────────────┐
│  EMAIL                              │
│  shikhirev.nn@phystech.edu          │
│                                     │
│  Содержимое:                        │
│  - Задача №                         │
│  - Текст вопроса                    │
│  - Ответ студента                   │
│  - Правильный ответ                 │
│  - Описание неточности              │
│  - Timestamp                        │
└─────────────────────────────────────┘
```

---

## Преимущества этой архитектуры

✅ **Frontend на GitHub Pages** — бесплатный хостинг с HTTPS  
✅ **Backend на Railway** — бесплатный tier ($5/месяц credits)  
✅ **Email через Gmail** — бесплатно до 500 писем/день  
✅ **Автодеплой** — push в GitHub → автоматический деплой  
✅ **Безопасность** — credentials только на Railway, не в коде  
✅ **Масштабируемость** — Railway автоматически масштабирует  

---

## Мониторинг

### Railway Dashboard предоставляет:

- **Metrics** — CPU, RAM, Network usage
- **Logs** — все console.log из `server.js`
- **Deployments** — история всех деплоев
- **Variables** — управление переменными окружения

### Рекомендуемые метрики для отслеживания:

- Количество запросов `/api/feedback` в день
- Время отклика API (<200ms норма)
- Успешность отправки email (100% в идеале)
- Использование credits Railway

---

## Следующие шаги (опционально)

1. **Rate Limiting** — ограничить количество запросов с одного IP
2. **Database** — сохранять обратную связь в базу данных
3. **Admin Dashboard** — веб-интерфейс для просмотра фидбека
4. **Webhook** — интеграция с Slack/Telegram для уведомлений
5. **Analytics** — трекинг популярных вопросов с ошибками

---

## Полезные ссылки

- 🚀 Railway: https://railway.app
- 📧 Gmail App Passwords: https://myaccount.google.com/apppasswords
- 📖 Railway Docs: https://docs.railway.app
- 💬 Railway Discord: https://discord.gg/railway
- 📦 Backend Repo: https://github.com/nicshik/mathstat-quiz-backend
- 🌐 Frontend: https://nicshik.github.io/mathstat-exam-quiz/

---

**Время развертывания:** ~5 минут  
**Сложность:** Низкая  
**Стоимость:** $0/месяц (бесплатные тиры)  
**Обслуживание:** Минимальное (автоматические обновления)
